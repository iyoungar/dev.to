#from django.forms import ModelForm

#from .models import UserPost

#class Postform(ModelForm):
  #  class Meta:
      #  model = UserPost
#        fields = ['title','body','tags']
 #       widgets = {
  #          'title': forms.CharInput(attrs={'class': 'space', 'placeholder': 'username '}),
   #         'body': forms.TextInput(attrs={'class': 'space', 'placeholder': 'email'}),
    #        'tags': forms.ChartInput(attrs={'class': 'space',}),
   #     }

